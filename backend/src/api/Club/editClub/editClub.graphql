enum ACTIONS {
    EDIT
    DELETE
}

type Mutation {
    editClub(
        name:String,
        bio:String,
        description:String,
        application_description:String,
        logoImage:String,
        clubImage:String,
        action: ACTIONS!
    ) : Club!
}
